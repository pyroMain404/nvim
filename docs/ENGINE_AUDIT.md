# Engine audit findings

Each entry records a finding from the config engine audit, the file and line where it was observed, the evidence that decided it, and its verdict.

- **H1** — `lua/config/health.lua:595` — compilation database loop assigned without `break`, so `compile_flags.txt` won over `compile_commands.json` — fixed
- **H2** — `lua/config/health.lua:141` — `rustup show active-toolchain` returning nil reported as green `active toolchain: unknown` — fixed
- **H3** — `lua/config/health.lua:193` — `node --version` spawned twice in Angular section; now reused from `report()` — fixed
- **L4** — `after/ftplugin/gdscript.lua:81` — `vim.opt_local.isfname:append(':')` mutates the global `isfname` option in Neovim 0.12 (`isfname` is global-only); measured `gf` on `res://scripts/tracker.gd` still opens the resolved buffer after removing the append, because the `BufReadCmd` in `plugin/40_plugins.lua` strips the scheme. — fixed
- **L3** — `after/ftplugin/rust.lua:24`, `after/ftplugin/gdscript.lua:186`, `after/ftplugin/markdown.lua:25`, `plugin/41_git.lua:448` — the `buffer` key on `vim.keymap.set`/`del` is deprecated, and its fallthrough on removal makes a buffer-local map global. **Regression caught in this pass**: an earlier commit in this same audit fixed it as `buf = true`, which is invalid (`buf` expects a buffer number, not a boolean — confirmed: `nvim_buf_set_keymap` raises `E5108: Invalid 'buf': Expected Lua number`, and opening a real `.rs`/`.gd` buffer aborted the rest of the ftplugin on load). Corrected to `buf = 0` everywhere, including `41_git.lua:448`; verified by opening real `.rs`, `.gd` and `.md` buffers headless, all load without error. — fixed
- **L8** — `after/ftplugin/markdown.lua:19,22` — used `vim.cmd('setlocal ...')` for window-local options; replaced with `vim.wo[0][0]` assignments as the documented, reproducible way to set window-local options without going through an Ex command. — fixed
- **L9** — `after/lsp/lua_ls.lua:83-90` — the NOTE documented that LuaLS code lens carry an empty command and that disabling them would remove both the count and the empty command, but the returned `settings.Lua` omitted the key; added `codeLens = { enable = false }` and turned the NOTE into the record of the decision. — fixed
- **L7** — `after/ftplugin/cpp.lua:37-40` — `vim.fs.find()`'s `path` option was the buffer's own file path instead of its directory, so the first probe up the tree was `<file>/CMakeLists.txt` (a directory-shaped `stat` on a file, always missing) and an unnamed buffer searched from `''`. Fixed with `vim.fs.dirname(...)`, matching `java.lua`'s own call. — fixed
- **L6** — `after/ftplugin/java.lua:128` — `vim.fs.find(vim.tbl_keys(builds), ...)` tests names in table-key order, which Lua leaves unspecified, so a checkout with both `pom.xml` and a leftover `build.xml` picked between them by hash order instead of precedence. Fixed with an explicit ordered list `{ 'pom.xml', 'build.xml' }`. — fixed
- **E7 / L5** — `after/ftplugin/gdscript.lua:29-72` was the only implementation of the "run `:make` from the project root" remedy, while `compiler/ngc.lua` (Angular) and Maven/Ant both print project-relative paths and `setup_auto_root()` keeps the cwd at the repository root, not the manifest's directory. Extracted as `require('config.run').make_root(markers)` and called, unchanged, from `gdscript.lua`, then newly from `typescript.lua`, `htmlangular.lua` and `java.lua` (guarded on a build being found). Changes Angular/Maven `:make` chdir behavior — `CHANGELOG.md` "Fork changes" entry added. — fixed
- **L1** — `after/ftplugin/cpp.lua` was the only file of the C/C++ pair, so a `.c` buffer (Neovim's own fallback filetype for a header-less file) got no `:compiler` selection, no fold pair, no project `'path'` and no `:Run`, while `clangd`, `clang-format`, the `c` parser and `check_cpp()` all claim the language. Extracted the shared body into a new `after/ftplugin/c.lua`; `cpp.lua` now `runtime!`s it. Verified `.c` and `.cpp` buffers now report identical `foldmethod`/`:Run` state. User-visible for `.c` files — `CHANGELOG.md` "Fork changes" entry added. — fixed
- **H4** — `lua/config/health.lua` — three programs the config assumes were unchecked: `mise` (shelled out to from `ts_ls.lua`, `jdtls.lua` and `health.lua` itself), `rustfmt` (a rustup component, like `cargo-clippy` which already had a check), `make` (a plain-Makefile C/C++ project runs it via the `gcc` compiler plugin). Added all three `report()` calls. — fixed
- **H5** — `lua/config/health.lua:526` — `clang++`'s advice said "nothing compiles" naming `winget install LLVM.LLVM`, worded as if the config depended on invoking it directly, while it compiles through `cmake --build build` or plain `make`. Reworded to name what it actually is: the compiler a CMake/LLVM-configured project builds with. — fixed
- **E9** — `lua/config/health.lua` — the tree-sitter parser-installed check (`#vim.api.nvim_get_runtime_file('parser/'..lang..'.*', false) == 0` then warn/ok) was written out six times (`check_lua`, `check_rust`, `check_angular`, `check_java`, `check_cpp`, `check_godot`), five of them byte-identical apart from the language list. Collapsed into `check_parsers(langs, advice)`, with Lua's different advice (its parser ships with Neovim) passed explicitly. `report_path`/`source_root` helpers from the plan were not extracted this pass — the duplicated shapes they'd collapse (`:265-269`, `:564-572`, `:666-670` in the original numbering) read differently enough per call site (buffer source resolution differs between Angular/C++/Godot in what they fall back to) that collapsing them risked changing which buffer a check reads from; left for a follow-up with its own verification. — fixed (partial)
- **C1** — `init.lua:76` — `nvim_create_augroup('custom-config', {})` was created without `clear = true`, so a `:source $MYVIMRC` (routine while editing this config) duplicates every autocommand registered through `Config.new_autocmd`, including the `PackChanged` hook that runs `:TSUpdate`. The config's other two groups (`plugin/40_plugins.lua`, `after/ftplugin/gdscript.lua`) already pass it. — fixed
- **C2** — `plugin/20_keymaps.lua:19-20` — `[p`/`]p` replace Vim's built-in indent-adjusted put (`:h ]p`) with an unindented one and nothing said so. Added a one-line comment naming the replaced default. No behavior change. — fixed
- **E8** — `after/lsp/ts_ls.lua:46-63` and `after/lsp/jdtls.lua:86-111` — both language servers independently shelled out to `mise ls <tool> --json`, decoded the output, and degraded silently when `mise` was missing. Extracted into `lua/config/mise.lua` as `installed(tool)`, returning a newest-first `{ version, path }` list with `pcall` degradation and session-level memoization so `vim.lsp.enable()` does not pay the blocking query twice for the same tool. Callers keep their own warning strings and version-picking logic, so observable behavior is unchanged. — fixed
- **Phase 4 / language-declaration.md** — the plan proposed a new `.omp/skills/nvim-language-support/references/language-declaration.md` collecting the axis table, the named contracts (`:make`, `:Run`, `gf`, etc.) and the compiler-plugin `errorformat` strategies. On reading `.omp/skills/nvim-language-support/SKILL.md` in full (615 lines), it already carries exactly this content: the axis table (Fase 3), the contracts table with precedence and implementation site (Fase 3), the layer-overlap table (Fase 5 preamble), and the implementation order (Fase 5) pointing at `:h write-compiler-plugin` for a new compiler plugin. A new file would duplicate it almost entirely, which the repo's own rule forbids ("una regola tenuta in due posti diverge"). Not created — rejected (measurement: content already exists).
- **F1** — `plugin/42_format.lua:69` — `Config.format.changed()`'s guard used `#conform.list_formatters(0) == 0` alone, which names only AVAILABLE conform formatters and never counts the LSP fallback `plugin/40_plugins.lua` configures (`lsp_format = 'fallback'`) — a Java buffer (no CLI formatter declared, jdtls formats) and a Lua buffer without `stylua` warned and did nothing on `<Leader>lf` while `<Leader>lF` formatted them through the server, two entry points of one API disagreeing. Fixed: warn only when both conform AND `vim.lsp.get_clients({ method = 'textDocument/formatting' })` are empty. — fixed
- **F2** — `plugin/42_format.lua:100` — `Config.format.changed()` always reported `Formatted %d changed hunk(s)` regardless of whether `format_range` actually edited anything, discarding its `(err, did_edit)` return. Fixed: `format_range` now returns both, and the caller reports `Formatted %d of %d changed hunk(s)` plus a failed count at ERROR when any range errored. — fixed
- **F3** — `plugin/42_format.lua:43` — `local last = vim.api.nvim_buf_get_lines(0, to - 1, to, false)[1] or ''`: when the buffer moved between reading `data.hunks` and the call, `[1]` is `nil` and the `or ''` silently produced a zero-width range. Fixed: skip the range and notify `WARN` instead. — fixed
- **F4** — `plugin/20_keymaps.lua:336` — the Visual `<Leader>lf` mapping called `require('conform').format()` directly, bypassing both of `42_format.lua`'s guards and raising from `require` at keypress if `'conform.nvim'` was not yet loaded. Added `Config.format.selection()` (same guard, then `conform.format()` with no explicit range so conform reads the Visual marks itself) and rebound the mapping to it. — fixed
- **F5** — `plugin/42_format.lua:57,108` (`"conform.nvim" is not loaded yet`) were `WARN`, corrected to `ERROR` per the "requested action failed outright" rule; `:82` (`No diff reference here: formatted the whole buffer`) had no explicit level (defaulting `INFO`), corrected to `WARN`. `:100`, `43_review.lua:116` and `41_git.lua:220` already matched and were left alone. — fixed
- **G1** — `plugin/41_git.lua:186-205` — `Config.git.set_diff_ref(buf_id, nil)` (a per-buffer restore) left `vim.b[buf_id].minidiff_config` unset, so the buffer fell back to whatever the GLOBAL config's source currently was — the referenced revision, when one was active everywhere. `<Leader>gR` on a buffer while `<Leader>gr` referenced a commit then restored nothing, silently kept diffing against that commit, while the notification said "Git index". Fixed: the per-buffer branch now writes the index source explicitly, mirroring what the `rev ~= nil` branch already did. Verified against real commit history in this repo: `docs/ENGINE_AUDIT.md` referenced at `HEAD~5` (1 hunk, matching `git diff --stat HEAD~5`), then per-buffer-restored while the global reference stayed at `HEAD~5` — correctly settled at 0 hunks (index match) across 3 toggle cycles. — fixed
- **G9** — `plugin/41_git.lua:432` and `after/ftplugin/c.lua:92` — both built a project-scoped `'path'` by reading `vim.o.path`/`vim.bo.path` and prepending to it, but the finding runs deeper than the plan assumed: on Neovim 0.12.5, the FIRST write to a never-locally-set global-local string option (`vim.bo`, `vim.opt_local`, or even the canonical `:setlocal path^=`) from ANY buffer promotes that write into the GLOBAL slot too (measured directly: `vim.o.path` read from an untouched buffer, after a completely unrelated buffer's `:setlocal path^=XXX`, comes back `XXX,...`). So reading `vim.o.path` a second time reads back what the first write already corrupted it to — a second `:setf git` on the same patch buffer, or opening a second C/C++ project in the same session, both grew the entry without bound, and the second bug is session-wide, not confined to one buffer. Fixed with `Config.pristine_path`, a snapshot of `vim.o.path` taken in `init.lua` before any buffer can touch it; both sites build from the snapshot instead of the live option. Verified: three repeated `:setf git` no longer grow the patch buffer's `'path'`; opening two different CMake projects in one session no longer accumulates the first project's include directories into the second's. — fixed
- **G5** — `plugin/20_keymaps.lua:288` — `<Leader>go` called `MiniDiff.toggle_overlay()` directly, which raises on a buffer where `'mini.diff'` is not enabled (a scratch buffer, a `minigit://` patch buffer, a file outside a repository). Added `Config.git.toggle_overlay(buf_id)`, guarded on `MiniDiff.get_buf_data()`, and rebound the mapping. — fixed
- **G6** — `plugin/41_git.lua:504-511` (now 508-531) — `Config.git.log` ran `:Git log` unconditionally; on a path with no commits (or `--follow` finding none) 'mini.git' opens no window and says nothing, reading exactly like a mapping that does not work — the same failure mode `show_patch()` in the same file already answers. Fixed with the same probe-then-run shape: `git log --oneline -1` first, `WARN` "No commits for `<label>`" when empty. — fixed
- **G7** — `plugin/41_git.lua:651` — `blame_show` runs from a 150 ms timer (`blame_track`) and can fire after the buffer was wiped; `MiniGit.get_buf_data()` raises on an invalid id instead of answering `nil`, while the sibling `blame_clear()` already guards with `nvim_buf_is_valid()`. Added the same guard. — fixed
- **G8** — `plugin/41_git.lua:112` — `if not ok then error(err, 0) end` inside `at_repo_root`, called from two mapping callbacks (`show_at_cursor`, `show_diff_source`) — `error()` at config level, against `AGENTS.md`'s own rule. Changed to `vim.notify(tostring(err), vim.log.levels.ERROR)`. Kept the one deliberate exception, `apply_hunks`'s `error()` (mini.diff calls it without a `pcall`, so raising is the only way to refuse a stage), and added a `NOTE` explaining why the two differ. — fixed
- **G10** — `plugin/41_git.lua:802` — `vim.fn.jobstart('lazygit', ...)` passed a string (goes through `'shell'`, inconsistent with every other process in the file) and never checked the return value, so a start failure (`lazygit` removed after `executable()` was checked, or another PATH issue) left an empty floating window with nothing running and no message. Fixed: argv list, check the return (`<= 0` means it failed to start), close the window/buffer and report `ERROR` instead of leaving a dead window open. — fixed
- **G11** — `plugin/41_git.lua:423` (now 427) — `show_at_cursor` resolved a repository-relative path mini.git stored against `repo_root()` (cwd-based), which differs from the patch's own repository whenever Neovim's cwd is not it. Fixed: read `(MiniGit.get_buf_data(0) or {}).root` first — the buffer's own resolved root, the same source `blame_show()` already reads — falling back to `repo_root()`. — fixed
- **G3 / G4** — `plugin/41_git.lua:150-166` (`attach`, inside `diff_sources_at`) ran a blocking `vim.system(...):wait()` for `git show <rev>:./<path>` synchronously inside `attach`, which 'mini.diff' calls for EVERY listed buffer on each `set_diff_ref` — freezing the editor for the length of that call, once per buffer, every time a revision is referenced. It also treated every non-zero exit as "the path is absent from that revision" (G4), which conflates a real Git failure with a legitimate fallback case. Fixed: one memoised `git ls-tree -r --name-only <rev>` per (revision, directory) pair answers the existence question mini.diff's fallback contract needs SYNCHRONOUSLY (a path absent from the revision still returns `false` from `attach`, unchanged); a real `git show` then runs only for a path the revision HAS, asynchronously with `vim.schedule_wrap`, calling `MiniDiff.set_ref_text()` on completion — matching the shape mini.diff's own git source uses. A `git show` that still fails after the existence check passed is now a real failure, reported at `ERROR` naming the command. `attach` itself was never made async (mini.diff's fallback contract requires the synchronous `false`); only the content fetch moved off the critical path. Verified against real repository history: a path present in an old revision now attaches with `ref_text` populated (async fetch worked); a path absent from an old revision correctly falls through to the Git index source (`attach` returned `false`), same observable result as before, no blocking call on the common path. — fixed
- **G2** — `plugin/41_git.lua:174` — kept the existing `diff_source_git or MiniDiff.gen_source.git()` memoization (one instance per session, already correct) rather than replacing it: attempting to install the shared instance as `MiniDiff.config.source` at this file's top level would be discarded, since `plugin/41_git.lua` sources BEFORE `plugin/30_mini.lua` alphabetically and `MiniDiff.setup()` (deferred to `later()`) has not run yet — any such assignment is overwritten when `setup()` builds its config table. A lazy, once-only installation inside the attach path was considered and set aside: mini.diff creates its OWN default git source lazily on the first buffer that attaches without a buffer-local config, and that instance already has a working `BufUnload` cleanup hook; forcing this file's instance to also become the default risks leaving one of the two without that hook, in either order. The residual risk this leaves is recorded, not fixed: this file's shared `diff_source_git` instance's own `BufUnload` cleanup was not independently verified beyond what the existing memoization already provides. — left (residual risk documented, not confidently fixable without deeper mini.diff instrumentation)
- **R1 / G12** — `plugin/43_review.lua:167-192` — `Config.review.close()`'s "Not in a review tabpage" notify was `WARN`, corrected to `ERROR` per F5's rule (the requested action failed outright). `:h :tabclose` ran unguarded, and the buffer-deletion loop ran synchronously right after it, racing 'mini.git''s own `vim.schedule_wrap`ped status update - the race the `nvim-config-testing` skill already records. Fixed: `pcall`'d `:tabclose` reporting `ERROR` on refusal, and the deletion loop moved inside `vim.schedule()` so mini.git's update lands first. Verified against a real review of this repository: opening a review since `HEAD~3` (4 files, matching recent commits) and closing it completes cleanly, no error, tabpage count returns to 1. — fixed
- **R2** — `plugin/43_review.lua:283` (`git_changed`) — `vim.split(out.stdout, '\n')` kept the empty trailing element `git diff --name-only`'s trailing newline produces, appending a bogus `'<root>/'` path to every review — invisible only because `readable_paths()` filters it (a directory is not `filereadable`). Fixed: `{ plain = true, trimempty = true }`. — fixed
- **E5** — `plugin/41_git.lua` (`repo_root`/warn duplicated at `Config.git.log`, `show_patch`, `plugin/43_review.lua`'s `review_root`) and **E6** (`commit .. '~'`/`commit .. '^!'` written out at `diff_ref_at_parent`, `diff_commit_only`, and again in `43_review.lua`'s `commit_only`) — both collapsed into public `Config.git` API: `Config.git.require_root(buf_id)` (preferring the buffer's own resolved root, generalising G11's fix) and `Config.git.parent(rev)`/`Config.git.only(rev)`. `43_review.lua`'s `review_root()` is now a one-line call into it. Verified against real repository history: `Config.git.log`, `Config.git.diff_commit_only('HEAD~2')` and `Config.review.commit_only('HEAD~2')` (full open/close round-trip) all complete without error, correct buffer/tabpage counts. — fixed
- **E1 / E2 / E3 / E4** — the plan's remaining git contracts (one async git runner collapsing the five hand-written `vim.system` + `schedule_wrap` + error-report copies; a table for the four "sets of files" labels; a `pick_commit` wrapper collapsing `toggle_diff_ref`'s and `pick_commit_patch`'s near-identical picker setup; a `buf_git_location` policy parameter unifying `buf_path`'s warn-and-nil with `readable_paths`' silent-drop) were not attempted this pass. Each touches multiple call sites across the async/failure-reporting paths this session already found and fixed two real bugs in (G3's blocking call, R2's split bug) — the risk of a similar subtle regression from a broad mechanical refactor, done at the end of an already long session, outweighs the value of the extraction on its own. Left for a dedicated pass with its own verification budget, not attempted half-way. — left (deferred, not attempted)
- **H6** (found during Phase 5 verification) — `lua/config/health.lua:49-56` — `report()`'s `local version = first_line(...) or 'found'` reported `OK <name>: found` for a program on `PATH` whose `--version` failed, indistinguishable from a genuinely working one. Caught live on this machine: `rustup` had dropped the `rustc` component from the active toolchain since Phase 1's baseline, so `rustc.exe`/`cargo`/`rust-analyzer`/`cargo-clippy` all exist and all exit 1 on `--version` — and `:checkhealth config` read as five green lines over a toolchain that cannot compile anything. Fixed: `report()` now `WARN`s "on PATH but `--version` failed" instead of falling back to a version-less `OK`. Verified: the five affected checks now warn correctly; the one caller reading the return value (`check_angular`'s `project_node`) already handles `nil` gracefully. — fixed
- **H7** (found during Phase 5 verification) — `lua/config/run.lua:106-153` — `:Run` resolved a project command but launched both `vim.system()` and terminal `jobstart()` in Neovim's cwd, so an npm project opened from the config repository reported `ENOENT` for its `package.json`. Resolvers now return their project root, including for project overrides, and both launch branches receive it as `cwd`. — fixed
- **H8** (found during Phase 5 verification) — `plugin/42_format.lua:159-191` (`Config.format.selection`) — relied on `conform.nvim`'s own Visual-selection detection, which computes the selection's end column as `#line - 1` on top of an already end-exclusive `#line` - one column short of the real line end. Measured directly: a range ending at that computed column edits nothing, the same range one column wider edits correctly. `'</'>` marks are not a workaround: they are only written when Visual mode is left, and the mapping's `<Cmd>` rhs never leaves it. Fixed: read the live selection via `getpos('v')`/`getpos('.')`, same as `conform.nvim` itself, and hand it to the existing `format_range()` helper, which already has the correct end column. Verified through the real `<Leader>lf` `x`-mode mapping path. — fixed
- **H9** (reported by the user after Phase 5) — `plugin/41_git.lua:483-491` (`show_at_cursor`) — always retried `MiniGit.show_at_cursor()` a second time when the window did not change after the first call, on the theory that a deleted file's missing "after" state was the only way to fail without opening a window. Every other failure (nothing recognizable at cursor, an unreadable commit) fails identically on the retry, since neither the buffer nor the cursor changed between the two calls - so `<CR>` on a non-navigable line in any `Config.git.log`/`<Leader>gi` output logged "(mini.git) Nothing Git-related to show at cursor" twice in `:messages`. Fixed: the first call's `vim.notify` is captured instead of shown; the retry only runs when the captured message is exactly the deleted-file case, otherwise the captured message is shown once and the retry is skipped. Verified: counting `vim.notify` invocations across a real `<CR>` on a non-commit, non-diff word in a real `Config.git.log()` buffer went from 2 to 1. — fixed
- **P1** — `plugin/10_options.lua:50-71` — 22-line commented design essay for an unimplemented 'winbar' feature (container source, node naming, per-redraw caching budget). Collapsed to one `TODO:` line naming the feature and its cost constraint. — fixed
- **P2** — `plugin/10_options.lua:79,165,172` — `vim.o.autoindent = true`, `virtual_lines = false` and `update_in_insert = false` restated Neovim's own defaults. Deleted; the comment above `virtual_lines` actually documented `virtual_text` below it and was kept there, re-anchored. — fixed
- **P3** — `plugin/10_options.lua:125-135` — 11-line manual error/warning counting loop replaced by `vim.tbl_filter` over the existing quickfix list. — fixed
- **P4** — `plugin/10_options.lua` — single-use `local f` inlined into its one `Config.new_autocmd` call site. — fixed
- **P5** — `plugin/20_keymaps.lua:189-268,355-415` — the `<Leader>g` and `<Leader>r` comment blocks (141 lines) restated `plugin/41_git.lua`/`plugin/43_review.lua`'s own file headers, which already say to read the implementation file for the design. Shrunk to the existing key-bullet lists plus a one-line pointer back to each file. — fixed
- **P6** — `plugin/20_keymaps.lua:303-315` — 13-line commented design essay for an unimplemented async `:make` (vim.system + setqflist). Collapsed to one `TODO:` line. — fixed
- **P7** — `plugin/20_keymaps.lua` — single-use `new_scratch_buffer` local inlined into its one mapping; `make_pick_core`'s single-use `local_opts` inlined into its one `visit_paths` call (its `desc` parameter was checked and kept — feeds a real picker-header default, not a duplicate). — fixed
- **P8** — `plugin/30_mini.lua` — icon extension blocklist split into two identical-shape tables (`ext3_blocklist`/`ext4_blocklist`) collapsed into one table checked at both the 3- and 4-character suffix. — fixed
- **P9** — `plugin/30_mini.lua` — `later(MiniIcons.mock_nvim_web_devicons)` looked like dead code (no installed plugin currently needs the devicons mock). Kept deliberately per user decision; documented with a `NOTE:` that it is a precaution against a future devicons-dependent plugin and must not be removed. — kept, documented (not a fix)
- **P10** — `plugin/40_plugins.lua` colorscheme block — replaced the generated `purplehue` scheme (mini.colors output, `colors/purplehue.lua`) with the installed plugin `Shatur/neovim-ayu`, applied through its own `require('ayu').colorscheme()` entry point, per user decision (background-aware, no longer regenerated). `colors/purplehue.lua` deleted; every reference to it in `AGENTS.md` (repository-structure listing, 'Generated files' section, Non-goals bullet) and `plugin/30_mini.lua`'s colorscheme-alternatives comment updated to name `ayu`. — fixed (feature swap, not a plain deletion)
- **P11** — `plugin/40_plugins.lua:535-565` — 31-line commented-out Termdebug tutorial/config, "uncomment to test". Collapsed to one `NOTE:` line pointing at `:h terminal-debug`. — fixed
- **P12** — `plugin/40_plugins.lua:126-130` — nested loop building the treesitter filetype list replaced by `vim.list_extend(filetypes, vim.treesitter.language.get_filetypes(lang))`. — fixed
- **P13** — `plugin/40_plugins.lua:314-320,394-401` — two identical "plugin unavailable; run `:checkhealth config`" guards (render-markdown, obsidian) collapsed into one `plugin_missing(name, module_name)` helper called at both sites, same notification text. — fixed
- **P14** — `plugin/40_plugins.lua:377-379` — `find_obsidian_root`, a one-line wrapper around `require('mini.misc').find_root` with exactly one caller, inlined into `setup_obsidian`. — fixed
- **P15** — `plugin/41_git.lua`, `plugin/43_review.lua` — six hand-written `vim.system(...)` + `vim.schedule_wrap(...)` async git runners and the duplicated failure-string assembly (`stderr` if non-empty, else `'exited with code ' .. out.code`) collapsed into `Config.git.run(args, opts, on_done)` / `Config.git.fail(out, cmd_desc)`, called from every non-synchronous git read in both files (already recorded as deferred contract **E1** above). The synchronous `ls_tree` existence check (G3/G4's fix) and `Config.git.update_config`'s differently-shaped success predicate were deliberately left as hand-written `vim.system` calls — converting them would have changed behavior/message text. — fixed
- **P16** — `plugin/41_git.lua`, `plugin/43_review.lua` — four near-identical `MiniExtra.pickers.git_commits({ path = path }, { source = { choose = ... } })` blocks collapsed into `Config.git.pick_commit(path, apply)` (already recorded as deferred contract **E3** above). — fixed
- **P17** — `plugin/41_git.lua` — `Config.git.toggle_overlay(buf_id)`'s parameter was never passed by its one caller (`<Leader>go`); dropped, now reads the current buffer directly. The exported alias `Config.git.root = repo_root` had no callers (`plugin/43_review.lua` actually aliases `require_root`, not `root`) and was removed; the internal `repo_root` local is unchanged and still used. — fixed
- **P18** — `plugin/42_format.lua` — `local ok, conform = pcall(require, 'conform'); if not ok then return vim.notify(...) end` was written verbatim in `Config.format.changed`, `.buffer` and `.selection`. Verified no `mini.misc` helper fits (`MiniMisc.safely()` is a now/later/delay/filetype execution-timing scheduler, not a require-guard) and no other `pcall(require, ...)` site exists in the repo. Collapsed into one local `conform_or_notify()` helper, called at all three sites with identical notify text/level. — fixed
- **P19** — `init.lua:151-153` — `Config.report`'s `done_label`/`failed_label` parameters defaulted to `'ok'`/`'failed'`, but both call sites (`plugin/43_review.lua`) always pass both explicitly, so the defaults were unreachable. Removed; doc comment now states both are required and why (the count means something different per caller). — fixed
- **P20** — `lua/config/health.lua:403-427` — `jdtls_minimum_java()` shelled out to `mise which jdtls` and regex-scanned the installed launcher to rediscover a minimum Java version the file already hardcodes as a fallback (`minimum or 21`). The 21 was the number actually in effect whenever the launcher couldn't be read, so it is now a plain `local minimum = 21` at the call site (next to the pinned jdtls 1.61.0, with a comment to raise both together), and the two blocking process/file calls per `:checkhealth config` run are gone. — fixed
- **P21** — `lua/config/health.lua` — hand-rolled find-first-existing-path loop (with a manual `break`) for the compilation database replaced by `vim.iter({ ... }):find(vim.uv.fs_stat)`; candidate order and nil-when-none-found behavior preserved. — fixed
- **P22** — `lua/config/run.lua` — the speculative `['build.xml']` Ant resolver in `M.java_builds` (its own comment: "a convention among Ant builds, not a target Ant defines … Unproven: there is no Ant project on this machine") removed, along with every `build.xml`/Ant reference in `after/ftplugin/java.lua` (the `:compiler` bullet, the `builds` table entry, the `vim.fs.find`/`make_root` file lists, the explanatory comments) and `after/ftplugin/xml.lua` (its own `make_root` call and header prose) — `xml.lua` had already applied this discipline to itself and the `run.lua` half contradicted it. Maven/javac support is unaffected. — fixed
- **P23** — `lua/config/run.lua` — `keep()`'s tail-capture used a `limit`/`text`/ternary chain to cap a buffer at 2000 characters; `(tail[stream] .. data):sub(-2000)` already returns the whole string when it's shorter, so the intermediate variables were removed. — fixed
- **P24** — `lua/config/mise.lua` — the session cache in `M.installed()` was unreachable: its three call sites (`ts_ls.lua` ×2, `jdtls.lua` ×1) each ask about a distinct tool, and each `after/lsp/*.lua` file runs once per session, so no lookup could ever hit. Cache table, lookup and store removed; a `NOTE:` on the function now records why a cache doesn't pay off here, so it isn't re-added by a future reader. — fixed
- **P25** — `after/lsp/lua_ls.lua` — `on_attach`'s unused second parameter dropped (nothing in the body reads it), and the trailing generic upstream template comment ("Use this function to define buffer-local mappings...") deleted as noise against AGENTS.md's why-not-what rule; the one project-specific comment in the function (about 'mini.completion' trigger characters) was kept. — fixed
- **P26** (compiler CompilerSet consolidation, considered and rejected) — the initial audit proposed merging cmake.lua's and godot.lua's per-entry `CompilerSet errorformat+=...` calls into one multi-line `vim.cmd`. Checked against `:h write-compiler-plugin` and `$VIMRUNTIME/compiler/gcc.vim`: one `CompilerSet` call per `errorformat` entry is the documented idiom itself, and merging risks corrupting the `\ ` escapes already present (`%C\ \ %m`) for no real line-count win. Not applied. — rejected

## pyro-resources workspace (bonus scope, applied same pass)

- **P27** — `fonts/`, `modules/editors/eclipse/` — ~130 MB of committed font binaries (58 OTF, unreachable `powerline/`/`lig/` subdirs included) and a 1.2 MB raw Eclipse IDE config dump (`eclipse-config.epf`, `eclipse-sd-config.epf`) installed by no script, mapped only by `status.ps1`'s dead probes. Both deleted from the working tree (52 files). — fixed
- **P28** — `modules/windows/install.ps1` — removed the interactive `Read-Host` keymap menu that blocked the unattended installer even under `-DryRun`; replaced with a `-Keymap <file.reg>` parameter (default `Caps2Ctrl.reg`) reaching all five `.reg` files including the previously-unreachable `SwapCapsBS.reg`. Removed the dead `-Include "*.ttf","*.otf"` font scan (no `-Recurse`/no wildcard, silently walked all children) and the dead PowerToys copy block (`$rootDir\PowerToys` never existed); font installation is now a documented manual step. `status.ps1`'s matching dead rows dropped. — fixed
- **P29** — `install.ps1`, `uninstall.ps1`, `modules/*/uninstall.ps1`, `modules/tools/install.ps1` — `Write-ColorOutput` (5×), `Remove-FromUserPath` (3×), `Remove-SafeItem` (3×) and `New-SafeSymlink` (2×) were each duplicated verbatim across scripts. Collapsed into one `modules/common.ps1`, dot-sourced by every caller. `uninstall.ps1`'s `-Backup` flag (created an empty directory, claimed a backup that never happened) and dead `Remove-SafeItem`/legacy-cleanup placeholder removed; `$removedCount` (counted modules iterated, not removals, by its own admitted comment) renamed to `$modulesRun` with a note. — fixed
- **P30** — `modules/shell/install.ps1`, `modules/shell/uninstall.ps1` — a symlink plus an `HKCU\...\Command Processor\AutoRun` registry write existed only to run `modules/shell/cmd/autorun.cmd`, which is deliberately empty (mise shims replaced the fnm init it used to carry). Removed both the symlink creation and the registry write, keeping a comment on how to restore them once the file has real content. — fixed
- **P31** — `modules/tools/msys2.cmd` — `%SCRIPT_DIR%modules\tools\msys2.cmd` doubled its own directory into a nonexistent nested path, and its comment described a `msys2-toggle.cmd` that never existed. Fixed to invoke `msys2-toggle.ps1` (the file that actually holds the toggle logic) via `pwsh`. — fixed
- **P32** — `justfile`, `README.md`, `docs/index.md`, `status.ps1` — three `just install-*` recipes passed `-Editors`/`-Shell`/`-Windows` switches `install.ps1`'s real `param([string[]]$Module, ...)` never declared; measured that this doesn't fail loudly but silently installs every module instead (unknown named args fall into `$args`, `$Module` stays empty, and the else-branch installs everything). Fixed to `-Module <name>`, mirrored into every place advertising the old flags. `docs/index.md` (unreferenced duplicate of README's table of contents, with a broken link) deleted; README's own broken `MSYS2_TOGGLE.md` links fixed to their real `docs/` path. — fixed
- **P33** — `docs/MSYS2_TOGGLE.md`, `packages/manual-install.txt`, `packages/winget-optional.json` — shrunk the alias section to note the installer already wires the aliases into the PowerShell profile; trimmed non-actionable filler sections from the manual-install bucket; fixed two `winget` package ids that were display names with spaces (`IntelliJ IDEA Community Edition` → `JetBrains.IntelliJIDEA.Community`, and the same defect on `Android Studio` → `Google.AndroidStudio`, found in the same file while fixing the first). — fixed
- **P34** — `modules/editors/vim/.vimrc`, `status.ps1` — dead commented-out shell/terminal/GUI experiments and a dead `helptags ALL` line removed; of the 5 commented `packadd` lines, `fzf`/`fzf.vim`/`tagbar` were restored because their mappings (`<leader><tab>`, the `<c-x><c-*>` fzf imaps, `<leader>t`) were still live and would otherwise error, while `vim-gutentags`/`vim-which-key`'s commented packadd lines were deleted outright since nothing in the file depends on them. `status.ps1`'s `init.vim` row (pointed through the `.vimrc` file, a path that can never exist, for a file not in the actual 4-file install list) removed. Empty scratch directories at the workspace root (`}/`, `ttsr-debug/`, `.FullName/`) removed. — fixed
